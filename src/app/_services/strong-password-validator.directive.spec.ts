import { StrongPasswordValidatorDirective } from './strong-password-validator.directive';

describe('StrongPasswordValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new StrongPasswordValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
